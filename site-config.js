/**
 * Coffee+ site configuration (edit for production).
 * - siteOrigin: used for canonicals / OG URLs in static HTML (already set per page).
 * - ga4MeasurementId: set to your GA4 ID (e.g. G-XXXXXXXX) to enable analytics + ecommerce events.
 */
window.COFFEEPLUS = {
    /** Production deploy URL; update if you add a custom domain. */
    siteOrigin: 'https://coffeewebsite-ebon.vercel.app',
    ga4MeasurementId: ''
};
